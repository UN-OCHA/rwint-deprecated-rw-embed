embed:
  image: unocha/nodejs
  environment:
    DNSDOCK_NAME: embed
    DNSDOCK_IMAGE: embed
  ports:
    - "3000:3000"
  volumes:
    - /code/rw-embed/container/root/opt/:/opt/
    - /code/rw-embed/logs:/var/log/rw-embed
    - /code/rw-embed:/var/www/html
  hostname: embed.vm
