embed:
  image: unocha/nodejs
  environment:
    DNSDOCK_NAME: embed
    DNSDOCK_IMAGE: embed
  volumes:
    - /code/rw-embed/nodejs/root/opt/:/opt/
    - /code/rw-embed:/var/www/html
  hostname: embed.vm
